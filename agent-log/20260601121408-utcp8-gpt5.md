# 20260601121408-utcp8-gpt5

## 用户原始 Prompt

```text
打开project.json无效
```

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`ef63fcc95f4995b39f73c8e89cc5917e4eef6d08`
- 同步：已执行 `git pull`，结果为 `Already up to date.`
- 任务结束时是否执行提交：否

## 任务执行时间

- 开始：2026-06-01 12:12:00 UTC+8
- 结束：2026-06-01 12:14:08 UTC+8

## 已阅读上下文

- 用户本次原始 prompt
- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `DESIGN.md`
- 最新执行日志
- renderer 项目打开与保存流程、project core、类型定义和测试

## 对话与行动记录

- 确认浏览器模式下 `Open Project` 仅触发隐藏 JSON 文件输入，后续只有占位提示，没有读取或恢复项目。
- 实现浏览器项目恢复流程：选择项目 JSON 后，再选择项目引用的 PO/POT 或 CSV 源文件，以重新授权文件访问。
- 支持 File System Access API 的浏览器使用文件句柄恢复源文件，并保留句柄供后续直接写回。
- 不支持 File System Access API 的浏览器使用隐藏文件输入完成两步恢复。
- 为项目 JSON 本身保留浏览器文件句柄：首次 `Save Project` 选择位置，后续保存直接覆盖；`Save Project As` 重新选择位置。
- 恢复 CSV 项目时应用 JSON 中保存的 CSV mapping。
- 恢复时校验项目格式，避免 PO/POT 项目误选 CSV，或 CSV 项目误选 PO/POT。

## 完成工作

- 修复浏览器预览中 `Open Project` 无效的问题。
- 补充浏览器项目 JSON 的保存覆盖语义。
- 更新浏览器项目恢复说明。

## 更新的需求 ID

- `[FILE-A-007]`

## 更新的 README 或 DESIGN 章节

- README：补充浏览器重新打开项目时需要重新授权选择源文件。
- DESIGN：未修改。

## 验证方式

- `npm test`：通过，1 个测试文件，共 9 个测试。
- `npm run build`：通过。
- `curl.exe --noproxy "*" http://127.0.0.1:5173/`：返回 `HTTP 200`。

## 备注

- 浏览器安全模型不允许仅凭 JSON 中记录的路径静默读取本地源文件，因此重新打开项目时必须再次选择源文件。
