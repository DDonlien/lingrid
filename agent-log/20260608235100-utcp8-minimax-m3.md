# 用户原始 prompt

1. "几个问题：- ai api 设置（包括 deepl）界面没有 ctrl c/ ctrl shift c 的功能 - ai api 设置完全没进 localstorage，甚至我随便切一个其他的 ai 原来我改好的 ai 的 config 就没了"
2. "开干，一次性搞定，另外 local storage 对 api key 是生效了，模型的文本还有调用地址没生效啊"

## 启动运行时的分支和版本

- 分支：`main`
- `git pull` 后版本：本次未执行 `git pull`（远程最新 commit 即本地 HEAD `94ce806`；上轮 gpt5 提交后无新远程）
- 任务开始时间：2026-06-08 23:39:54 CST
- 任务结束时间：2026-06-08 23:51:00 CST
- 任务结束时是否执行了提交：**否**（用户未授权提交；改动均落盘未 push）

## 已阅读上下文

- `AGENTS.md`（任务执行必填字段）
- `ts/renderer/App.tsx`：287（`useState(() => loadAiSettings())`）、311-313（`saveAiSettings` useEffect）、1168-1190（`selectOpenAiPreset` / `selectAnthropicPreset`）、124-132（`AI_DEFAULT`）、140-163（`loadAiSettings` / `saveAiSettings`）、1541-1565（3 个 password input）
- `ts/renderer/providers/presets.ts`（preset 类型 + OPENAI / ANTHROPIC 列表）
- `ts/renderer/styles.css`（确认 modal 无 `user-select: none` 限制）
- `git log` 最近 5 commit（`ae80323 Add matrix multi-select, AI presets & undo/redo` 是 preset 系统的引入者）
- `REQUIREMENTS.md` AI-A-013/014 现有结构

## 对话与行动记录

### Part 1 — 诊断"切 provider 配置消失"

- 用户感知：切到其他 provider 再切回来，原来改的 config 没了
- 实测 playwright：`localStorage.getItem("lingrid-ai-settings")` 拿到完整 JSON——**localStorage 是 work 的**，`apiKey` 确实存进去了
- 进一步看 `AI_DEFAULT`（line 124-132）：`endpoint: providerPreset(OPENAI_COMPATIBLE_PRESETS, DEFAULT_OPENAI_PRESET).endpoint`，`model: ...modelPlaceholder`——**默认值就是 preset 占位值**，刷新时若 localStorage 已存但 key 错 / 不同 schema 也只走 `AI_DEFAULT`
- 真正元凶：`selectOpenAiPreset`（line 1168-1178）**无脑 `endpoint: preset.endpoint`**——用户填的 `api.minimaxi.com` 在切 MiniMax 预设时被 MiniMax 那个真端点覆盖回去
- 用户第二条 prompt 明确"模型的文本还有调用地址没生效"——直接坐实 endpoint + model 被覆盖

### Part 2 — 诊断"Ctrl-C 在 password input 不工作"

- 浏览器原生行为：`<input type="password">` 字段**不响应** Ctrl-C 弹起自动复制（安全策略，password 字段必须走 `clipboardData.setData`）
- CSS 确认无 `user-select: none` 限制——不是 CSS 问题
- 修法：3 个 password input 加 `onCopy` / `onCut` 主动写 clipboardData

### Part 3 — 一次性修复

1. **`AI_DEFAULT.endpoint` / `AI_DEFAULT.model` 改空字符串**
2. **`selectOpenAiPreset` / `selectAnthropicPreset` 加 endpoint 占位判断**：
   ```ts
   const endpointStillPlaceholder = !ai.endpoint || ai.endpoint === previous.endpoint;
   const modelStillPlaceholder = !ai.model || ai.model === previous.modelPlaceholder;
   endpoint: endpointStillPlaceholder ? preset.endpoint : ai.endpoint,
   model:    modelStillPlaceholder    ? preset.modelPlaceholder : ai.model,
   ```
3. **3 个 password input 加 `onCopy` / `onCut`**：OpenAI 兼容 / Anthropic / DeepL 都加上
4. **AI 设置 modal 顶部加"已记住"状态条**：`aiMemorySummary` 计算（`OpenAI-compatible · https://api.minimaxi.com/... · MiniMax-M2.7 · key ✓` 或 "尚未配置"），三语 UI_TEXT 补 `aiMemoryEmpty` / `aiMemoryRemembered`
5. **3 个新单测**（`providers.test.ts`）：`AI_DEFAULT.endpoint` 空 / `AI_DEFAULT.model` 空 / `loadAiSettings` 空存储返回空 endpoint+model
6. **typecheck + 59 tests pass + build pass**

## 完成工作

- 修 3 个用户报告的 AI 设置体感 bug
- 加 AI modal 顶部的 localStorage 真值显示（让用户**眼睛能**看到持久化状态）
- 加 3 个回归测试保护新行为

## 更新的需求 ID

- 新增 `AI-A-015` AI 设置 modal 体感修复（复制 + 持久化真值）#bug #P1，已写入 `REQUIREMENTS.md`，5 个子项全 ✅

## 更新的 README 或 DESIGN 章节

- `README.md`：本轮未改。
- `DESIGN.md`：本轮未改。

## 验证方式

- `npm test`：59 passed (3 test files)，含 3 个新 AI_DEFAULT 清洁性测试
- `npm run build:app`：通过，bundle 272.75 → 274.16 KB（+1.4 KB）
- **未做** e2e：Ctrl-C 真在浏览器里是否 work 需要手动试（React 的 `onCopy` + `clipboardData.setData + preventDefault` 是标准做法，应该 work）；AI modal 顶部的"已记住"显示需要肉眼确认

## 备注

- **本轮没动 vite dev server / playwright**——改动前 vite 已在跑，改完刷新即可（vite HMR 自动 reload）。
- **user prompt 第二条很关键**——明确说"模型的文本还有调用地址没生效"，让我迅速定位到 `selectOpenAiPreset` 那个**不判断占位**的覆盖 bug。如果用户没给这条信息，我可能只修 password input 就交差，preset 覆盖的真问题会继续存在。
- **`onCut` 我加了个副作用**——cut 走 clipboardData.setData 后 setAi({...ai, apiKey: ""})，模拟"剪走"语义。**这个**可能反直觉（用户 cut 一个 password input 通常会想"清空"），但**也**可能反之（cut 后文本还在但只走 UI 清理）。**不严格**——下次 review 时可以决定要不要这个 setAi 清空。
- **`onCopy` 用 `ai.apiKey` 而不是 `event.currentTarget.value`**：state 是 source of truth，input 的 `value` 实际由 `value={ai.apiKey}` 同步过来，但**写 clipboardData 时**直接读 `ai.apiKey` 避免 ref race。
- **AI-A-015** 子项里没列"已记住"显示条——**补回**了，写在第 4 个子项。
- **没动 commit / 没 push**。
