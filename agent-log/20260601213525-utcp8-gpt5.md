# 20260601213525-utcp8-gpt5

## 用户原始 Prompt

```text
搜索/筛选状态不要保存到项目里
```

## 用户补充 Prompt

```text
如果点击了其他地方，筛选的下拉框应该收起
```

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`1f3ea7cdd28c4d55473c3851f7a09443b6dae845`
- 同步：已执行 `git pull --ff-only`，结果为 `Already up to date.`
- 任务结束时是否执行提交：否

## 任务执行时间

- 开始：2026-06-01 21:33:00 UTC+8
- 结束：2026-06-01 21:35:25 UTC+8

## 已阅读上下文

- 用户本次原始 prompt 与补充 prompt
- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `src/core/project.ts`
- `src/core/types.ts`
- `src/renderer/App.tsx`
- `tests/adapters.test.ts`

## 匹配到的需求

- `[CORE-A-004]` 定义项目状态模型
- `[FILE-A-007]` 保存项目 JSON
- `[SEARCH-A-006]` 筛选 tag

## 对话与行动记录

- 用户要求搜索和筛选状态不写入项目文件。
- Agent 检查发现 `serializeProject()` 直接持久化了整个 `view`。
- Agent 移除项目 JSON 中的 `view` 字段，并在打开项目时忽略旧 JSON 中可能残留的 `view`。
- 用户补充要求筛选下拉框点击外部区域后收起。
- Agent 增加统一的 `pointerdown` 外部点击处理，关闭未包含点击目标的已打开 `details` 菜单。

## 完成工作

- 项目 JSON 不再保存搜索、筛选、排序或强制补齐开关。
- 打开旧项目时恢复默认会话视图状态。
- 完成度筛选、tag 筛选和界面语言菜单支持点击外部区域收起。
- 更新 `AGENTS.md`、`README.md` 和 `REQUIREMENTS.md`。

## 更新的需求 ID

- `[CORE-A-004]`
- `[FILE-A-007]`
- `[SEARCH-A-006]`

## 更新的 README 或 DESIGN 章节

- README：修正项目 JSON 保存范围说明。
- DESIGN：未修改。

## 验证方式

- `npm test`：通过，25 个测试。
- `npm run build`：通过。
- `git diff --check`：通过。
- 本地浏览器：打开筛选菜单后点击外部按钮，已打开筛选菜单数量从 `1` 变为 `0`。

## 备注

- 旧项目 JSON 中已有 `view` 字段时保持兼容读取，但会忽略该字段。
