# 20260601135715-utcp8-gpt5

## 用户原始 Prompt

```text
左下角的All Changes Saved也应该响应对应的内容：PO not saved, Project not saved（如实展示）
```

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`ef63fcc95f4995b39f73c8e89cc5917e4eef6d08`
- 同步：已执行 `git pull`，结果为 `Already up to date.`
- 任务结束时是否执行提交：否

## 任务执行时间

- 开始：2026-06-01 13:56:31 UTC+8
- 结束：2026-06-01 13:57:15 UTC+8

## 已阅读上下文

- 用户本次原始 prompt
- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `DESIGN.md`
- 最新执行日志
- footer 状态栏、源文件 dirty 和 project dirty 判断

## 对话与行动记录

- 将左下状态栏从单一 changed 单元格计数改为复用两套独立 dirty 状态。
- 源文件 dirty 时显示 `PO not saved` 或 `CSV not saved`。
- 项目 JSON 状态 dirty 时显示 `Project not saved`。
- 两者同时存在时使用 `·` 并列展示。
- 两者均干净时显示 `All changes saved`。
- 新增 `sourceFormatLabel` helper，统一 PO/CSV 展示名称判断。

## 完成工作

- 左下状态栏如实展示源文件和项目文件保存状态。

## 更新的需求 ID

- `[UI-A-006]`

## 更新的 README 或 DESIGN 章节

- DESIGN：补充左下状态栏的独立保存状态展示规则。

## 验证方式

- `npm test`：通过，1 个测试文件，共 17 个测试。
- `npm run build`：通过。
- `git diff --check`：通过，仅有现存 LF/CRLF 提示。
- `curl.exe --noproxy "*" http://127.0.0.1:5173/`：返回 `HTTP 200`。

## 备注

- 状态栏与顶部两个保存按钮共用同一组 dirty 判断。
