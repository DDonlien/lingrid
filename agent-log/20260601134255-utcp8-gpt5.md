# 20260601134255-utcp8-gpt5

## 用户原始 Prompt

```text
表头需要排序功能，点击在4种排序间切换，同时只能以一列激活排序其他列跟随排序
- 未完成优先
- 完成优先
- 内容增序（此时未完成的放在最后）
- 内容降序（此时未完成的放在最后）
```

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`ef63fcc95f4995b39f73c8e89cc5917e4eef6d08`
- 同步：已执行 `git pull`，结果为 `Already up to date.`
- 任务结束时是否执行提交：否

## 任务执行时间

- 开始：2026-06-01 13:41:00 UTC+8
- 结束：2026-06-01 13:42:55 UTC+8

## 已阅读上下文

- 用户本次原始 prompt
- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `DESIGN.md`
- 最新执行日志
- project view、矩阵 UI 投影、表头样式和测试

## 对话与行动记录

- 在 `ProjectView` 中新增单列翻译排序状态。
- 新增 `nextSortMode`，点击同一语言表头时按四态轮转：未完成优先、完成优先、内容升序、内容降序。
- 新增 `sortedEntries`：内容升降序时空译文固定放最后；完成度排序保持同组内原始顺序。
- 矩阵语言表头改为可点击按钮；激活列显示对应图标和轻量强调。
- 点击另一个语言表头会切换激活列，并从未完成优先开始。
- 排序只影响 UI 投影，不修改源文件 entry 顺序。
- 新增排序回归测试。

## 完成工作

- 语言表头支持单列四态排序。
- 排序状态随项目视图保存到项目 JSON。

## 更新的需求 ID

- `[UI-A-002]`

## 更新的 README 或 DESIGN 章节

- DESIGN：补充语言表头排序视觉规范。

## 验证方式

- `npm test`：通过，1 个测试文件，共 15 个测试。
- `npm run build`：通过。
- `curl.exe --noproxy "*" http://127.0.0.1:5173/`：返回 `HTTP 200`。
- 新增回归测试：四态排序轮转、空值位置和内容升降序。

## 备注

- 当前没有“取消排序”第五态；切换到其他语言列会替换当前激活排序列。
