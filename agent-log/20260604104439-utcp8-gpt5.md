# 用户原始 prompt

整理资产：
- 根目录保留所有必须的内容
- 所有ts的内容创建一个/ts文件夹往里放
- 所有electron的内容创建一个/electron文件夹往里放
然后完成P2

## 启动运行时的分支和版本

- 分支：`main`
- `git pull` 后版本：`2c8844676211e973721159fbb6f43698aca1fcec`
- `git pull` 结果：Already up to date.

## 时间

- 任务开始时间：2026-06-04 10:35:00 CST
- 任务结束时间：2026-06-04 10:44:39 CST
- 任务结束时是否执行了提交：否

## 已阅读上下文

- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `DESIGN.md`
- 最近日志列表与 P2 相关需求
- 当前仓库目录、`package.json`、tsconfig、Vite 配置、Electron main/preload、测试入口

## 对话与行动记录

- 确认当前分支为 `main`，执行 `git pull` 并确认已最新。
- 检查 `REQUIREMENTS.md`，确认本次 P2 对应 `BACK-A-009`：Electron 桌面封装与安装包发布。
- 将浏览器/共享 TypeScript 源码从 `src/adapters`、`src/core`、`src/renderer` 移入 `ts/`。
- 将测试从 `tests/` 移入 `ts/tests/`。
- 将 Electron main/preload 从 `src/main`、`src/preload` 移入 `electron/`。
- 更新 `index.html`、`tsconfig.app.json`、`tsconfig.electron.json`、测试导入路径和 Electron packaged 资源路径。
- 安装 `electron-builder`，在 `package.json` 增加桌面打包配置、构建命令和发布命令。
- 执行 `npm run package:electron` 和 `npm run dist:electron`，生成本机 mac arm64 应用、ZIP 和 DMG。
- 将 `release/` 加入 `.gitignore`。
- 更新 `README.md`、`AGENTS.md`、`REQUIREMENTS.md`。

## 完成工作

- 根目录保留必要配置、入口、文档、fixtures、GitHub Actions、package 与 tsconfig。
- 新增 `/ts` 作为 TypeScript / React / core / adapter / tests 的统一目录。
- 新增 `/electron` 作为 Electron main/preload 的统一目录。
- P2 Electron 封装完成：`npm run package:electron` 生成 unpacked app，`npm run dist:electron` 生成 `release/` 下的平台分发产物。

## 更新的需求 ID

- `[BACK-A-009]` 完成 Electron 桌面封装与安装包发布：已勾选。

## 更新的 README 或 DESIGN 章节

- `README.md`：更新当前状态、目录结构、Electron 开发入口和 P2 打包发布命令。
- `AGENTS.md` 项目专用内容：更新当前阶段、Electron 发布命令和目录索引。
- `DESIGN.md`：未更新，视觉规范未变化。

## 验证方式

- `npm test`：通过，1 个测试文件，28 个测试通过。
- `npm run build`：通过，Vite 产物和 Electron TS 编译成功。
- `npm run package:electron`：通过，生成 `release/mac-arm64/Lingrid.app`。
- `npm run dist:electron`：通过，生成 `release/Lingrid-0.1.0-arm64.dmg`、`release/Lingrid-0.1.0-arm64-mac.zip` 及 blockmap。

## 备注

- `electron-builder` 提示当前使用默认 Electron 图标，并因未配置 notarization 跳过 macOS notarization；正式发布前需补充应用图标、Developer ID 证书与 notarization 配置。
- npm 输出 `Unknown user config "home"` warning，未影响安装、测试、构建或打包结果。
