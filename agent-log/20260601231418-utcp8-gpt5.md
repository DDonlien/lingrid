# 20260601231418-utcp8-gpt5

## 用户原始 Prompt

```text
修改：
- 全部tags改名叫Source Tag
- 全部勾选时，应该意味着所有tag都被选上
- 全部和下面的选项中应该有一道虚线隔开
- 增加一个默认的“空”选项对应没有tag的项目
```

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`1f3ea7cdd28c4d55473c3851f7a09443b6dae845`
- 同步：已执行 `git pull --ff-only`，结果为 `Already up to date.`
- 任务结束时是否执行提交：否

## 任务执行时间

- 开始：2026-06-01 23:06:00 UTC+8
- 结束：2026-06-01 23:14:18 UTC+8

## 已阅读上下文

- 用户本次原始 prompt
- `README.md`
- `REQUIREMENTS.md`
- `DESIGN.md`
- `src/core/project.ts`
- `src/renderer/App.tsx`
- `src/renderer/styles.css`
- `tests/adapters.test.ts`

## 匹配到的需求

- `[SEARCH-A-006]` 筛选 tag
- `[SEARCH-A-006-WORD]` 筛选 Word Tag

## 对话与行动记录

- 用户要求修正 Tag 筛选命名与全部选项语义，并增加无标签筛选。
- Agent 将 Source Tag 顶栏入口从 `全部 tags` 改为 `Source Tag`。
- Agent 将 Source Tag 与 Word Tag 的 `全部` 改为显式选择全部已知 tag 和 `空`，取消勾选时清空筛选。
- Agent 增加 `空` 哨兵筛选值，并区分真实存在但没有 Word Tag 的单元格与源文件中不存在的灰置单元格。
- Agent 在 `全部` 与具体选项之间增加低对比虚线。

## 完成工作

- Source Tag 与 Word Tag 筛选菜单均提供 `全部`、虚线、`空` 和具体标签列表。
- Source Tag 的 `空` 匹配没有 Source Tag 的条目。
- Word Tag 的 `空` 匹配指定语言范围内真实存在但没有 Word Tag 的单元格。
- 勾选 `全部` 时同步选中全部具体标签与 `空`。
- 更新 `README.md`、`REQUIREMENTS.md` 和 `DESIGN.md`。

## 更新的需求 ID

- `[SEARCH-A-006]`
- `[SEARCH-A-006-WORD]`

## 更新的 README 或 DESIGN 章节

- README：补充 Tag 筛选的 `全部` 与 `空` 语义。
- DESIGN：补充虚线分隔和 `Empty` 视觉规范。

## 验证方式

- `npm test`：通过，28 个测试。
- `npm run build`：通过。
- `git diff --check`：通过。
- 本地浏览器：Source Tag 菜单显示 `全部`、虚线和 `空`；点击 `全部` 后菜单内 `8 / 8` 个 checkbox 全部勾选。

## 备注

- 没有勾选任何 Tag 时，筛选保持关闭；勾选 `全部` 时筛选结果等价于显示全部有标签与无标签项。
