# 20260601204707-utcp8-gpt5

## 用户原始 Prompt

```text
检查一下为什么“保存.po"还是无效
```

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`1f3ea7cdd28c4d55473c3851f7a09443b6dae845`
- 同步：已执行 `git pull`，结果为 `Already up to date.`
- 任务结束时是否执行提交：否

## 任务执行时间

- 开始：2026-06-01 20:43:00 UTC+8
- 结束：2026-06-01 20:47:07 UTC+8

## 已阅读上下文

- 用户本次原始 prompt
- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `src/renderer/App.tsx`
- `src/core/project.ts`
- `src/core/browser-files.ts`
- `src/adapters/po.ts`
- `src/main/main.ts`
- `src/preload/preload.ts`
- `tests/adapters.test.ts`

## 匹配到的需求

- `[FILE-A-005]` 保存修改到原 `.po`
- `[FILE-A-009]` 支持当前 PO 另存
- `[FILE-A-010]` 保存前处理风险提示

## 对话与行动记录

- 用户反馈“保存 PO”仍然无效。
- Agent 同步仓库并检查现有保存链路。现有代码在 Electron 或支持 File System Access API 的浏览器中可以持有文件句柄并直接覆盖源文件。
- Agent 发现普通 `<input type=file>` 降级路径没有可写句柄，点击主保存只能失败；同时 PO/CSV 另存为没有合并仍聚焦输入框中的草稿。
- Agent 抽出草稿合并 helper，使主保存与另存为共用同一份序列化前数据。
- Agent 为无可写句柄的浏览器增加下载更新副本降级，并保留未保存状态，避免误导为已覆盖原文件。

## 完成工作

- 新增 `applyTranslationDrafts()`，统一保存前草稿合并。
- 修复 PO/CSV 另存为遗漏当前未失焦草稿的问题。
- 普通文件选择降级路径点击主保存时下载更新副本。
- 状态栏明确提示浏览器无法覆盖原文件，已下载更新副本；原文件未保存状态继续保留。
- 更新 `README.md`、`AGENTS.md` 和 `REQUIREMENTS.md`。

## 更新的需求 ID

- `[FILE-A-005]`
- `[FILE-A-009]`
- `[FILE-A-010]`

## 更新的 README 或 DESIGN 章节

- README：浏览器开发预览保存语义
- DESIGN：未修改

## 验证方式

- `npm test`：通过，21 个测试。
- `npm run build`：通过。
- 新增回归测试：当前仍聚焦单元格中的草稿会在保存或另存为序列化前合并进 `.po`。

## 备注

- 当前线程的内置 Browser 没有暴露可执行控制入口，无法直接点击用户已打开页面进行 UI 自动化复现。
- Electron 和支持 File System Access API 的浏览器仍直接覆盖已授权源文件；只有无法获得可写句柄的浏览器才下载更新副本。
