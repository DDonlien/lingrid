# 20260601221134-utcp8-gpt5

## 用户原始 Prompt

```text
把tag功能拆分成2层，在侧栏现有的tag下面增加额外一行
Source Tag：不变
Word Tag：增加到每个具体语言的具体单元格
在添加word tag后，应该在单元格的右上角（做一些padding）增加一个小小的竖条长方形彩色标签，hover应该显示所有tag，如果有多个tag应该是多个竖条，竖条的颜色和tag的应该保持一致，顶栏目应该增加一个筛选功能Word Tag，它和“未完成”一样，应该有二级分类（一级word，二级是对应的语言）
```

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`1f3ea7cdd28c4d55473c3851f7a09443b6dae845`
- 同步：第一次 `git pull --ff-only` 因 GitHub TLS 连接失败暂停；用户要求继续后再次执行，结果为 `Already up to date.`
- 任务结束时是否执行提交：否

## 任务执行时间

- 开始：2026-06-01 22:00:00 UTC+8
- 结束：2026-06-01 22:11:34 UTC+8

## 已阅读上下文

- 用户本次原始 prompt
- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `DESIGN.md`
- `src/core/types.ts`
- `src/core/project.ts`
- `src/renderer/App.tsx`
- `src/renderer/styles.css`
- `tests/adapters.test.ts`

## 匹配到的需求

- `[TAG-A-000]` Tag 与标记
- `[SEARCH-A-003]` 搜索 tag
- `[SEARCH-A-006]` 筛选 tag

## 对话与行动记录

- 用户要求把 tag 拆分为 Source Tag 与 Word Tag。
- Agent 保留原 entry 级 tag 作为 Source Tag，并在 `TranslationCell` 上增加 Word Tag。
- Agent 增加 Word Tag 项目 JSON 持久化、侧栏编辑、单元格右上角彩色竖条和按 tag + 语言范围筛选。
- Agent 使用稳定 hash 生成颜色，相同 Word Tag 在不同单元格保持一致色彩。

## 完成工作

- 侧栏现有 tag 更名为 `Source Tag`，下方增加 `Word Tag`。
- Word Tag 绑定具体 source 与语言单元格。
- Word Tag 保存到项目 JSON 的 `wordTags`，默认不写入 `.po` 或 `.csv`。
- 单元格右上角增加带 padding 的细小彩色竖条；多个 Word Tag 展示多个竖条；hover 显示全部标签。
- 顶栏新增 Word Tag 筛选菜单，支持 Word Tag 多选和语言范围多选。
- 搜索框同时匹配 Source Tag 与 Word Tag。
- 批量替换、草稿提交和 changed 状态清理保持 Word Tag 不丢失。
- 更新 `AGENTS.md`、`README.md`、`REQUIREMENTS.md` 和 `DESIGN.md`。

## 更新的需求 ID

- `[TAG-A-000]`
- `[TAG-A-006]`
- `[TAG-A-007]`
- `[SEARCH-A-003]`
- `[SEARCH-A-006-WORD]`

## 更新的 README 或 DESIGN 章节

- README：补充两层 Tag 与项目 JSON 保存边界。
- DESIGN：补充 Word Tag 筛选和单元格彩色竖条规范。

## 验证方式

- `npm test`：通过，27 个测试。
- `npm run build`：通过。
- `git diff --check`：通过。
- 本地浏览器：为当前单元格输入 `#review #todo` 后出现 `2` 条彩色竖条，hover 标题为 `#review #todo`。
- 本地浏览器：顶栏 Word Tag 菜单显示 tag 列表和语言二级列表；选择 `#review + 日本語` 后矩阵筛选为 `1` 行。

## 备注

- Source Tag 和 Word Tag 都属于项目状态，不属于 PO/CSV 翻译正文。
