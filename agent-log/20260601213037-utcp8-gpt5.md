# 20260601213037-utcp8-gpt5

## 用户原始 Prompt

```text
我测试并发现问题了
不是po写入无效
是2个文件可能会出现source不对齐，比如两个文件各自只有679条，但同时打开就有710条了
几个问题：
你现在合并条目的依据是什么？
这种情况客观来说也是有可能的，正确的处理情况应该是：
1. 根本没这个条目的对应语言的单元格应该灰置无法修改（也不纳入统计和filter）
2. 可以给一个强制写入的开关（顶栏）开启后这些单元格则是开启写入和统计，保存po时应该不仅仅保存msgtext，而是把整个段落的内容（id什么都，我不知道po里有什么）都写入
```

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`1f3ea7cdd28c4d55473c3851f7a09443b6dae845`
- 同步：已执行 `git pull --ff-only`，结果为 `Already up to date.`
- 任务结束时是否执行提交：否

## 任务执行时间

- 开始：2026-06-01 21:22:00 UTC+8
- 结束：2026-06-01 21:30:37 UTC+8

## 已阅读上下文

- 用户本次原始 prompt
- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `src/adapters/po.ts`
- `src/adapters/csv.ts`
- `src/core/project.ts`
- `src/core/types.ts`
- `src/renderer/App.tsx`
- `src/renderer/styles.css`
- `tests/adapters.test.ts`

## 匹配到的需求

- `[FILE-A-005]` 保存修改到原 `.po`
- `[FILE-A-010]` 保存前处理风险提示

## 对话与行动记录

- 用户确认此前误判为写盘失败的问题实际是多语言 PO 条目集合不对齐。
- Agent 检查确认当前 PO 合并稳定 key 为 `msgctxt + msgid + msgid_plural`，矩阵使用各文件条目并集。
- Agent 修复缺失语言单元格被当作普通空译文的错误语义。
- Agent 增加顶栏 `强制补齐` 开关。开启后允许编辑缺失单元格，并在保存目标 PO 时从其他语言条目克隆完整 block 后追加。

## 完成工作

- 缺失语言单元格默认灰置、禁用，不计入该语言完成度统计和完成度筛选。
- 修复 `applyTranslationDrafts()` 静默制造缺失语言单元格的问题。
- PO parser 为矩阵条目保留可复用的完整 block 模板。
- PO writer 在强制补齐时追加完整条目，保留注释等元数据，并替换目标译文。
- 顶栏增加 `强制补齐` 开关。
- 更新 `AGENTS.md`、`README.md` 和 `REQUIREMENTS.md`。

## 更新的需求 ID

- `[FILE-A-005]`

## 更新的 README 或 DESIGN 章节

- README：增加多语言 PO 条目错位与强制补齐说明。
- DESIGN：未修改。

## 验证方式

- `npm test`：通过，24 个测试。
- `npm run build`：通过。
- `git diff --check`：通过。
- 本地浏览器：确认顶部 `强制补齐` 按钮显示。
- 回归测试：构造目标语言缺少条目的错位 PO，验证默认统计排除缺口；开启强制模式后纳入统计；保存后目标 PO 追加完整 block 并保留注释元数据。

## 备注

- Electron 打包仍未开始。
- 当前强制补齐针对 PO 文件；CSV 保持原有映射行写入语义。
