# 20260601120904-utcp8-gpt5

## 用户原始 Prompt

```text
我open第二个.PO时，没有增加列，而是重开了一个列，这不对（下面显示imported 1 file 而不是 2 file）
并且表头词汇一直是GAME而不是对应的语言
```

## 用户补充 Prompt

```text
save按钮现在会尝试下载一个新的.PO文件而不是直接修改打开的文件
```

```text
标题里的按钮应该改成如下名称和顺序：
Save PO | Save PO As | Save Project | Save Project As
如果导入的源文件是CSV，PO自动改成CSV
不支持csv和po混合到一个项目里
先不要管electron
```

```text
不用electron应该也有办法改本地文件的（在浏览器里）
```

```text
不行你就还是electron
```

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`ef63fcc95f4995b39f73c8e89cc5917e4eef6d08`
- 同步：已执行 `git pull`，结果为 `Already up to date.`
- 任务结束时是否执行提交：否

## 任务执行时间

- 开始：2026-06-01 11:56:00 UTC+8
- 结束：2026-06-01 12:09:04 UTC+8

## 已阅读上下文

- 用户本次原始 prompt 与补充 prompt
- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `DESIGN.md`
- 最新执行日志
- PO adapter、project core、renderer、main/preload 和测试

## 对话与行动记录

- 修复 PO header 的 `Language:` 识别正则。此前实际回退到文件名，导致同名 `GAME.po` 被错误合并为 `GAME` 列。
- 将追加导入改为函数式状态更新，提示改为累计 source file 数。
- 浏览器文件输入在导入后清空 value，允许再次选择相同文件。
- 调整保存按钮名称和顺序；CSV 项目自动显示 CSV。
- 禁止 PO/POT 与 CSV 混合进入同一项目，并将规则提到 core 层。
- 浏览器预览接入 File System Access API：支持时保留文件句柄，主保存直接覆盖原文件；不支持时提示重新打开或使用 Electron。
- 浏览器写回前检查 `lastModified`，避免覆盖外部修改。
- Electron main process 使用 `basename()` 处理 Windows 文件名。
- Electron 下载因初次依赖安装超时残留在后台；根据用户要求暂不处理 Electron，已停止残留安装链。

## 完成工作

- 同名 PO 文件按 header 语言生成独立列。
- 累计导入数量显示正确。
- 保存按钮顺序为 `Save PO/CSV`、`Save PO/CSV As`、`Save Project`、`Save Project As`。
- PO/POT 与 CSV 项目互斥。
- 支持现代浏览器直接写回已打开本地文件。

## 更新的需求 ID

- `[FILE-A-004]`
- `[FILE-A-005]`

## 更新的 README 或 DESIGN 章节

- README：补充浏览器开发预览的 File System Access API 保存语义。
- DESIGN：未修改。

## 验证方式

- `npm test`：通过，1 个测试文件，共 9 个测试。
- `npm run build`：通过。
- `curl.exe --noproxy "*" http://127.0.0.1:5173/`：返回 `HTTP 200`。
- 新增回归测试：两个同名 `GAME.po` 根据 header 合并为 `zh-CN` 与 `ja` 两列。
- 新增回归测试：PO/POT 可共存，PO/POT 与 CSV 不可混合。

## 备注

- 浏览器直接写回依赖 File System Access API。支持该 API 时必须通过工具栏 `Open` 打开文件以保留句柄。
- Electron 运行时二进制尚未安装完成，本次按用户要求暂不继续处理。
