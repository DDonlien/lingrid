# 20260608164830-utcp8-gpt5

## 用户原始 prompt

实现 3 个 AI 的功能，Batch 会给别的 Agent 实现

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`7411e05d7840b70404751232073116b7eb325692`
- 启动时已执行：`git pull --ff-only`，结果为 Already up to date

## 任务时间

- 任务开始时间：2026-06-08 16:36:00 CST
- 任务结束时间：2026-06-08 16:48:30 CST
- 任务结束时是否执行了提交：否

## 已阅读上下文

- `AGENTS.md`
- `REQUIREMENTS.md`
- `ts/core/types.ts`
- `ts/renderer/App.tsx`
- `ts/renderer/styles.css`
- `ts/renderer/providers/openai-compatible.ts`
- `ts/renderer/providers/deepl.ts`
- `ts/tests/providers.test.ts`

## 对话与行动记录

- 用户要求实现 3 个 AI 需求，并明确 Batch 交给其他 Agent。
- 本次仅实现：
  - `[AI-A-020] 扩展 AI provider 设置结构`
  - `[AI-A-030] 确认 prompt template 变量真实生效`
  - `[AI-A-040] 支持 AI 翻译批量填充`
- 未实现 `[BATCH-B-010] 点击矩阵单元格以外的位置时取消当前单元格选择`。
- 新增 OpenAI-compatible preset 列表，覆盖 OpenAI、Kimi / Moonshot、MiniMax、通义千问 / DashScope、豆包 / 火山方舟、Gemini、Claude OpenAI SDK compatibility 和自定义 OpenAI 兼容。
- 新增 Anthropic-compatible 预留列表，包含 Claude native、MiniMax Anthropic-compatible 和自定义 Anthropic-compatible，并保持禁用/待实现状态。
- 抽出 prompt template 渲染纯函数，确保 `{{language}}` 使用列显示名优先、缺失时 fallback 到 language id，`{{source}}` 使用当前行 source。
- 调整 AI 生成逻辑：
  - 单个选中单元格保持“生成建议 + 用户应用”的现有行为。
  - 多选单元格时，只翻译选区中可编辑且空置的译文单元格，并直接写入。
  - 无选中单元格时，支持翻译全部可编辑且空置的译文单元格，供后续取消选择功能接入。
  - 批量失败不回滚已成功写入的单元格，最终显示成功、跳过和失败数量。
- 更新 AI 设置弹窗展开区，让 provider preset 和 DeepL region 都使用同一套分段卡片样式。

## 完成工作

- 新增 `ts/core/ai.ts`：AI prompt template 渲染函数。
- 新增 `ts/renderer/providers/presets.ts`：AI provider preset 定义。
- 新增 `ts/tests/ai.test.ts`：prompt 变量渲染测试。
- 更新 `ts/core/types.ts`：扩展 `AiProvider` 与 `AiSettings`。
- 更新 `ts/renderer/App.tsx`：provider 设置 UI、preset 切换、AI 单格/多选/全空单元格翻译分发。
- 更新 `ts/renderer/styles.css`：provider 展开区卡片、禁用项和响应式样式。
- 更新 `REQUIREMENTS.md`：勾选 `[AI-A-020]`、`[AI-A-030]`、`[AI-A-040]` 及其子项。

## 更新的需求 ID

- `[AI-A-020]`
- `[AI-A-030]`
- `[AI-A-040]`

## 更新的 README 或 DESIGN 章节

- 本次未新增 README / DESIGN 内容。

## 验证方式

- `npm test`：通过，3 个测试文件，50 个测试。
- `npm run build`：通过，包含 Vite app build 与 Electron TypeScript build。
- `git diff --check`：通过。
- in-app Browser 打开 `http://127.0.0.1:5173/`，检查 AI 设置弹窗：
  - 顶层 provider 包含 OpenAI 兼容、其他兼容、DeepL。
  - OpenAI 兼容展开区包含 8 个预设。
  - DeepL 展开区沿用卡片式 Free / Pro 选择。

## 备注

- 本次未提交。
- 批量 AI 诊断只记录 provider、总数、写入数、跳过数和失败数，不记录 API key、source 原文或译文内容。
