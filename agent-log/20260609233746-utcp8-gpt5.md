# 执行日志

## 基本信息

- 用户原始 prompt：`实现phase 1.5`
- 启动分支：`main`
- 启动版本：`bdc3196`
- 任务开始时间：`2026-06-09 23:10:00 +08:00`
- 任务结束时间：`2026-06-09 23:37:46 +08:00`
- 任务结束时是否执行了提交：否

## 已阅读上下文

- `REQUIREMENTS.md`（Phase 1.5 需求范围）
- `ts/core/types.ts`
- `ts/core/project.ts`
- `ts/renderer/App.tsx`
- `ts/renderer/styles.css`
- `ts/tests/adapters.test.ts`

## 对话与行动记录

- 用户要求"实现phase 1.5"。经排查，Phase 1.5 包含四个 Epic：EDIT-A（单元格编辑状态追踪）、PERF-A（矩阵性能优化）、FILE-B（隐藏式项目文件）、FILE-C（最近项目历史）。
- FILE-C 已在先前提交中完全实现（最近项目下拉菜单、localStorage 持久化、单条移除/清除）。
- FILE-B 已在先前提交中基本实现（`.lingrid.json` 隐藏文件、文件夹一站式加载、`directoryHandle` 保留）。
- **EDIT-A 本次新增实现：**
  - 在 `TranslationCell` 类型中添加 `neverEdited?: boolean` 字段。
  - 修改 `ProjectView`，将 `changedOnly: boolean` 替换为 `editStatus: string[]` 多选筛选。
  - 修改 `serializeProject` 持久化 `neverEdited` 状态到项目 JSON。
  - 修改 `restoreProject` 恢复 `neverEdited` 状态。
  - 修改 `importDocuments`，首次导入时自动为所有新单元格标记 `neverEdited: true`。
  - 修改 `applyMatrixValue`、`applyMatrixValues`、`applyBatch`，在值实际变化时清除 `neverEdited`。
  - 修改 `filteredEntries`，支持按 `never-edited` / `changed` / `unchanged` 多选筛选。
  - 在矩阵单元格右上角添加 `.never-edited-marker` 灰色圆点角标。
  - 在详情侧栏 `translation` 字段标签右侧添加 `.never-edited-badge` 标记。
  - 重构顶部筛选栏，将原来的 `changedOnly` 按钮替换为 `editStatus` 多选下拉菜单（FilterMenu），支持三态多选。
  - 添加中/日/英三语 UI 文本：`editStatus`、`neverEdited`、`changed`、`unchanged`。
  - 添加对应 CSS 样式。
- **PERF-A 本次新增实现：**
  - 引入基础虚拟滚动（Virtual Scroll）机制：
    - 添加 `matrixWrapRef` 和 `scrollTop` state。
    - 监听 `matrix-wrap` 容器的 `scroll` 事件。
    - 计算 `visibleStart` / `visibleEnd` 索引（固定行高 48px + 5 行缓冲区）。
    - 只渲染 `virtualVisible` 范围内的行，通过顶部/底部占位 `<tr>` 保持滚动条总高度。
    - 多选状态仍保存在 JavaScript 内存中（`selectedMatrixCells`），不受虚拟滚动影响。
- 更新 `REQUIREMENTS.md` 中 Phase 1.5 需求验收状态：EDIT-A 全部完成，PERF-A 部分完成（虚拟滚动基础实现完成，输入框状态下放和 memo 组件拆分待后续优化），FILE-B/C 保持已完成。

## 完成工作

- 实现单元格"从未修改"状态追踪与持久化（EDIT-A）。
- 实现"从未修改"视觉角标与详情区标记（EDIT-A）。
- 重构"修改状态"多选筛选（EDIT-A）。
- 引入矩阵虚拟滚动基础实现（PERF-A-001）。
- 更新 REQUIREMENTS.md Phase 1.5 验收状态。

## 更新的需求 ID

- `EDIT-A-000` 及子项 `EDIT-A-001` ~ `EDIT-A-003`
- `EDIT-A-010` 及子项 `EDIT-A-011` ~ `EDIT-A-013`
- `EDIT-A-020` 及子项 `EDIT-A-021` ~ `EDIT-A-023`
- `PERF-A-000` 及子项 `PERF-A-001`（基础虚拟滚动）

## 更新的文件

- `ts/core/types.ts`
- `ts/core/project.ts`
- `ts/renderer/App.tsx`
- `ts/renderer/styles.css`
- `ts/tests/adapters.test.ts`
- `REQUIREMENTS.md`

## 验证方式

- `npm test`：通过，4 个测试文件，83 个用例全部通过。
- `npm run build`：通过（Vite app + Electron）。
- `git diff --check`：通过。

## 备注

- 版本更新决策：本次为 Phase 1.5 功能实现，涉及核心类型变更和 UI 重构，建议版本号升级至 `0.1.2`。
- PERF-A 中 `PERF-A-002`（输入框状态下放）和 `PERF-A-003`（memo 组件拆分）尚未实现，可作为后续优化项。
- PERF-A-001 中键盘导航到屏幕外行自动滚动对齐已通过 `moveFromCell` 的 `requestAnimationFrame` + `cellInputs.current.get(...)?.focus()` 部分实现，但尚未显式调用 `scrollToItem` 式滚动对齐。
