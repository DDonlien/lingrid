# 用户原始 prompt

你根目录里这些什么ts config什么的不能挪到ts下吗？这是最佳实践？

## 启动运行时的分支和版本

- 分支：`main`
- `git pull` 后版本：`2c8844676211e973721159fbb6f43698aca1fcec`
- `git pull` 结果：Already up to date.

## 时间

- 任务开始时间：2026-06-04 10:45:00 CST
- 任务结束时间：2026-06-04 10:47:28 CST
- 任务结束时是否执行了提交：否

## 已阅读上下文

- 当前工作区状态
- `package.json`
- 根目录 TS / Vite / Electron 配置文件
- 上一轮已更新的 README 与 AGENTS 目录索引

## 对话与行动记录

- 说明 `tsconfig*.json` 放根目录是常见做法，但在本项目“根目录只留必须内容、TS/Electron 资产分区”的整理目标下，专用配置可以移动到对应目录。
- 将浏览器/React/测试侧配置移动到 `ts/`：`tsconfig.json`、`tsconfig.app.json`、`tsconfig.node.json`、`vite.config.ts`。
- 将 Electron 编译配置移动到 `electron/tsconfig.json`。
- 更新 `package.json` scripts，显式指定 Vite 与 TypeScript 配置路径。
- 更新 `README.md` 与 `AGENTS.md` 目录索引。

## 完成工作

- 根目录不再保留 `tsconfig*.json` 或 `vite.config.ts`。
- `ts/` 内集中保存 TS/Vite 相关配置。
- `electron/` 内保存 Electron 编译配置。

## 更新的需求 ID

- 无新增需求 ID；属于上一轮资产整理的范围修正。

## 更新的 README 或 DESIGN 章节

- `README.md`：目录结构补充 `ts/tsconfig.json`、`ts/vite.config.ts` 和 `electron/tsconfig.json`。
- `AGENTS.md`：目录索引补充配置文件归属。
- `DESIGN.md`：未更新。

## 验证方式

- `npm test`：通过，1 个测试文件，28 个测试通过。
- `npm run build`：通过，`tsc -b ts/tsconfig.json`、`vite build --config ts/vite.config.ts`、`tsc -p electron/tsconfig.json` 均成功。

## 备注

- 保留根目录 `package.json`、`package-lock.json`、`index.html` 等工具入口和项目入口文件，因为它们仍是 npm/Vite/Electron/部署流程默认或必要入口。
- npm 仍输出 `Unknown user config "home"` warning，未影响验证结果。
